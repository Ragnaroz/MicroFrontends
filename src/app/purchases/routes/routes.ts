import { Routes } from "@angular/router";
import { PurchasesPageComponent } from '../purchases-page/purchases-page.component';

export const routes: Routes = [
    { path: '', component: PurchasesPageComponent },
  ];